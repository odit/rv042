./configure --host=${CROSS} AR="${AR}" ARFLAGS="rc" CC="${CC}" CPP="${CPP}" LIBS="${LIBS}" RANLIB="${RANLIB}" STRIP="${STRIP}" CFLAGS="${CFLAGS} -DNO_ASM" LDFLAGS="${LDFLAFGS}" --prefix="${ROOT}/usr" --sysconfdir="/etc"
make
make install